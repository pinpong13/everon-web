# Optional: set to enable real email delivery from the contact form.
# Get a free key at https://resend.com (sandbox sender works for testing).
RESEND_API_KEY=
# Optional verified sender. Defaults to onboarding@resend.dev (test only).
CONTACT_FROM="Everon Web <hello@yourdomain.com>"
